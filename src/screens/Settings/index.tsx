import React from 'react';
import { TopBar } from '../../components/TopBar';

export default function Settings() {
    return(
        <>
            <TopBar topButton="settings"/>
        </>
    )
}